name: monee
description: Personal finance app — Plaid-synced US bank accounts + manual tracking.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.0
  flutter_riverpod: ^2.6.1
  go_router: ^14.6.0
  fl_chart: ^0.69.0
  intl: ^0.19.0
  google_fonts: ^6.2.1
  url_launcher: ^6.3.0
  lucide_icons: ^0.257.0
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
